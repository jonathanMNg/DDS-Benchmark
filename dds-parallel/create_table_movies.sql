CREATE TABLE IF NOT EXISTS MOVIES(
                                id INTEGER,
                                title TEXT,
                                popularity REAL,
                                vote_average REAL,
                                budget INTEGER,
                                revenue INTEGER,
                                release_date NUMERIC
);
